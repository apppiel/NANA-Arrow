# REPORT_PLANNER.md — 기획자 보고
> **기획자(클로드 데스크탑)만 쓴다.** 작업 하나 끝날 때마다 맨 위에 새 항목 추가. 디렉터는 읽기만.
> 형식: `### W-### 완료 (날짜)` / 산출물 경로 / 디렉터 확인 요청 / 팀장 에디터 할 일

### W-008 완료 (2026-09-17)
**산출물**: docs/LEVEL_DESIGN.md **v0.3** §7 — 레벨 11~20 JSON + 보드 그림 + 요약표
- 전부 프로젝트 `LevelValidator.Validate` 오류 0, `Record` 전후 solution·minTaps 동일
- 11~15: 6×6 Basic+Long, n 8→12, depth 4→7 (11 휴식, 15 고비)
- 16~20: Frozen 1→3개, n 9→13, depth 4→6. 16 은 Frozen 이 처음부터 쏠 수 있는 휴식 레벨, 17~20 은 막힌 Frozen 을 포함해 얼음은 깨져도 발사는 Block 되는 상황을 학습
- 생성기를 무작위 배치 → 경로 위 사슬 배치로 바꿈 (무작위로는 6×6 에서 depth 5 이상이 거의 안 나옴). 11~100 역방향 생성기 설계에 참고 가능
- Lv16 튜토리얼 손가락 대상: `a9`

**디렉터 확인 요청**
1. Lv20 depth 6 (§3 목표 7 미달) — 화살표 수·점유율로 보완. 플레이 테스트 후 판단 제안

**팀장 에디터 할 일**: 없음 (문서만 변경). 레벨 파일 저장은 W-006 에디터 툴 이후

**참고**: W-004·W-007 은 이미 다른 기획자 세션에서 완료·보고되어 있어 이번에는 중복 작업하지 않음

### W-007 완료 (2026-09-17)
**산출물**: docs/UI_FLOW.md v0.1
- 전체 흐름도, 공통 규칙(팝업 1개씩, Android 뒤로가기 표), Boot / Main(+레벨 선택) / Game(HUD 배치도) 화면 구성
- 팝업 6종 문구·버튼: 클리어, 실패(광고 이어하기 / 다시하기, 광고 미준비·이어하기 소진·중도 닫기 처리), 설정(Main/Game 차이), 메인으로 확인, 응모 코드(W-009 결과로 확정), 종료 확인
- 튜토리얼 표(Lv 1·2·6·16·26) — `TutorialConfig` ScriptableObject 로 관리 제안
- **광고 연결 지점 표 (§8)**: UI 이벤트 `LevelCleared` / `RetryPressed` / `ContinueRequested` → AdsManager 판단 → 콜백 후 이동. 프로그래머 W-005·#16 이 바로 참고 가능
- 문구 키 표 30여 개, 사운드 리스트 13종

**디렉터 확인 요청** (UI_FLOW §11)
1. 진동 토글 추가 (제안: 추가, 기본 켜짐, 하트 감소 시만)
2. 설정 팝업의 `다시하기`는 실패로 세지 않고 광고 없음
3. 레벨 중간 상태 저장 없음 (앱 종료 시 그 레벨 처음부터)
4. v1 언어 한국어만
5. 이미 깬 레벨을 다시 클리어해도 전면 광고 카운트에 포함

**팀장 에디터 할 일**: 지금은 없음. 캔버스·팝업 프리팹 구조 제안은 UI_FLOW §12

### W-004 완료 (2026-09-17)
**산출물**
- docs/LEVEL_FORMAT.md **v0.4** — Long 외 타입(Basic / Frozen / Locked / Key)은 정확히 1칸 (좌표계 설명 + 검증 규칙 2). 프로그래머 가정 (1)과 일치
- docs/LEVEL_DESIGN.md **v0.2**
  - 레벨 1~10 을 프로젝트 `LevelValidator.Validate` 로 재검증 → 전부 오류 0
  - 레벨 5·8·9 의 `solution` 을 검증기 결과 순서로 교체 (정답은 같고, 같은 층 안의 순서만 다름). 이제 `Record` 해도 JSON 이 바뀌지 않음
  - §3 보드 크기 11 / 21 / 40 → 승인 반영
  - 레벨 10 은 이전 보고대로 조건 완화 후 재생성한 결과(9·10 순서 교체) 그대로 유지
- docs/ISSUES.md #4 완료 조건 문구를 위 내용에 맞게 수정 (등록 전이면 반영, 이미 등록됐으면 이슈 본문만 맞추면 됨)

**디렉터 확인 요청**: 없음

**팀장 에디터 할 일**: 없음 (문서만 변경)

### 이전 보고 — 이슈 목록 · LEVEL_DESIGN 1~10 (2026-09-17, 채팅 보고를 옮김)
**산출물**
- docs/ISSUES.md — GitHub 이슈 초안 21건(M1 코어 7 / M2 화면·흐름 8 / M3 수익·출시 6) + 기획 트랙 5건. 라벨·우선순위·상태·완료 조건 포함. 권장 순서: #2·#3 병합 → #4 → #5 → #7 → 화면
- docs/LEVEL_DESIGN.md v0.1 — 난이도 지표 4개(n, free0, depth, fill), 톱니형 곡선 원칙, 1~100 구간 계획, 레벨 1~10 JSON + 보드 그림. 1~10 은 제 시뮬레이터와 프로젝트 `LevelLoader`·`FireResolver` 양쪽으로 풀이 확인. 난이도 3개·2층(Lv1) → 9개·7층(Lv10), Lv6 은 Long 도입 휴식 레벨
- docs/CLAUDE_DESKTOP.md 다음 할 일 갱신

**그 전 작업 요약**
- LEVEL_FORMAT v0.1 예시 레벨이 순환 막힘으로 풀 수 없음을 발견 → v0.2 에서 교체, 해결 가능 판정을 탐욕 시뮬레이션으로 변경
- GAME_RULES v0.4 검토 7건 → v0.5 / v0.5.1 반영 확인, §9 HUD·§5 별점 잔여 불일치 지적 → v0.5.1 해결

**디렉터 확인 요청 (당시)** — 모두 처리됨
- 보드 크기 전환 11 / 21 / 40 → v0.5.2 승인
- CLAUDE.md 의 Undo·힌트 비용 잔재 → 제거됨
- 광고 SDK → AdMob + Unity Ads 확정
- 응모 코드 기존 코드 위치 → W-009 조사로 진행

**팀장 에디터 할 일**: 없음

