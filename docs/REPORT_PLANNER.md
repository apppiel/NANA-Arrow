# REPORT_PLANNER.md — 기획자 보고
> **기획자(클로드 데스크탑)만 쓴다.** 작업 하나 끝날 때마다 맨 위에 새 항목 추가. 디렉터는 읽기만.
> 형식: `### W-### 완료 (날짜)` / 산출물 경로 / 디렉터 확인 요청 / 팀장 에디터 할 일

### W-014 완료 · W-013 보류 (2026-09-17)
**W-014 산출물**: docs/ANALYTICS.md v0.1 (이슈 #26)
- 이벤트 18종: 레벨 진행 5 (level_start·clear·fail·quit, retry) / 광고 5 (continue_offer·request·granted, ad_interstitial, ad_rewarded_result) / 튜토리얼·기능 3 / 메뉴·보상 5
- 공통 파라미터 6개(level, level_name, board, arrows, is_replay, attempt), 사용자 속성 4개, Firebase 콘솔 보고서 6종, 구현 메모(`AnalyticsReporter` 한 곳에서만 전송)
- `level_start` 는 Firebase 권장 이벤트 이름과 맞추고 `level_name` 동봉. 자동 수집 `ad_impression` 은 직접 보내지 않음. 디바이스 ID·응모 코드 값 전송 금지

**W-013 보류 사유** — 아래 결정 후 바로 진행 가능
1. W-016 보고의 확인 요청 2번 `maxArrowLength` 상향, 3번 보드 구간 (21~30 을 8×10→8×11 로 계획)
2. W-015 경로형 검증기 머지 (W-013 은 프로젝트 LevelValidator 검증이 조건)

**디렉터 확인 요청**: 없음 (이벤트 이름은 그대로 확정해도 됨)
**팀장 에디터 할 일**: 없음

### W-012 완료 (2026-09-17) — UI_FLOW v0.2
**산출물**: docs/UI_FLOW.md **v0.2**
- GAME_RULES §11 결정 5건 반영 (⚠ 제거)
- §0 HUD 반영: 게임 화면 = 뒤로가기·다시하기(좌상단 원형) + 하트 3개. **레벨 번호·게임 중 설정 팝업 삭제**, 설정은 Main 전용(사운드·진동). HUD 다시하기 = 즉시 재시작·실패 미포함·광고 없음, 뒤로가기 = 메인으로 확인
- 레인 미리보기(길게 누르기), Block 레인 번쩍, 하단 말풍선 튜토리얼 반영
- §7 `TutorialConfig` SO 필드 정의(코드 초안) + 레벨별 항목 (Lv1 a2, Lv2 a3, Lv4 길게 누르기 안내, Lv16 a2)
- §9 문구 키 정리 (hud.level·settings.retry·settings.main 삭제, tut.* 6개)
- §12 **팀장용 단계별 조립 가이드**: 한글 TMP 폰트 준비, 캔버스 설정값, HUD·튜토리얼·팝업 6종·Main·레벨 선택·Boot 의 오브젝트 이름·앵커·크기·컴포넌트

**디렉터 확인 요청**
1. §12-8 스크립트 8건 (LivesView, TutorialPresenter, MainMenu, LevelSelectView, SettingsPopup, GameController 재시작/메인 이동, PopupBase 닫기 옵션, Strings SO) 프로그래머 배정 필요 — W-010 범위에 없음
2. 게임 중 설정이 없어져 **사운드를 게임 화면에서 끌 방법이 없음** (레퍼런스와 동일). 이대로 갈지
3. Lv4 튜토리얼 `화살표를 길게 누르면 가는 길이 보여요` 추가 (레퍼런스에 있는지 미확인, 기능 안내용 제안)

**팀장 에디터 할 일**: W-010 머지 후 UI_FLOW §12 순서대로 조립. 가장 먼저 §12-1 한글 폰트 (없으면 모든 문구가 네모로 나옴)

### W-016 완료 (2026-09-17) — 경로형 레벨 1~20
**산출물**
- `Assets/_Project/Levels/level_001~020.json` **덮어씀** (경로형, version 1 유지)
- docs/LEVEL_FORMAT.md **v0.5** — 경로 cells(꼬리→머리), dir = 마지막 세그먼트, Long 삭제, 검증 규칙 2 교체, `mask` 예약, 가로·세로 다른 보드 허용. 예시 레벨(Key·Locked·Frozen 포함)을 경로형으로 새로 만들고 검증
- docs/LEVEL_DESIGN.md **v1.0** (전면 재작성) — 레퍼런스 역산표, 지표 7개(총 경로 길이·꺾임 수 추가), 곡선 원칙, 보드 구간 제안, 1~20 요약표·보드 그림, 튜토리얼 메모, 생성기 메모
- 검증: main 의 검증기는 아직 직선 모델이라, LEVEL_FORMAT v0.5 규칙 0~5 를 그대로 구현한 기획 측 검사기로 20개 전부 통과 + 프로젝트 `LevelLoader.Parse` 로 스키마 파싱 확인

**레퍼런스 역산 결과 (스크린샷을 Unity 에서 픽셀 분석)**
| 장 | 보드 | 화살표 | 점유 | 최장 | 꺾임 |
|---|---|---|---|---|---|
| lv1 | 5×6 | 3 | 60% | 6 | 0 |
| lv2 | 5×5 | 3 | 68% | 7 | 4 |
| lv3 | 4×5 | 4 | 95% | 7 | 4 |
| lv4 | 8×10 | 5 | **100%** | **34** | 21 |

**레벨 1~20 요약**: 1 곧은 선 3개 → 2 첫 꺾임 → 3 꽉 찬 4×5 → 4~7 6×7·6×8 (점유 100%) → 8~10 7×8·7×9 (10 고비 깊이 6) → 11~15 7×9→8×10 (15 고비 깊이 7) → 16~20 8×10 Frozen 1~3 (20 고비 깊이 7). 점유율 3레벨부터 90~100%

**디렉터 확인 요청**
1. **GAME_RULES v0.6.1 에 v0.5.x 이전 문구가 되살아남** (v0.5 이전 판에 §0 을 붙인 것으로 보임)
   - §3 Key 행 없음 (v0.5 에서 추가했던 것)
   - §4 보드 크기가 11/21/40 확정안(v0.5.2)이 아님
   - §5 별점이 minTaps 기준 (v0.5.1 에서 잃은 목숨 기준으로 바꿨음)
   - §7 `coinPerClear`·별 보너스 (v0.5 에서 코인 삭제)
   - §9 상단 HUD(레벨·코인·설정)·하단 부스터 바 (§0 HUD 와도 충돌)
2. **`maxArrowLength` 12 → 상향 제안 (예: 40)**. 레퍼런스 Lv4 에 34칸 화살표. 이번 1~20 은 12 이하로 맞춤, 21 이후 긴 경로에 필요
3. **보드 구간 변경 승인** (LEVEL_DESIGN §4): 정사각 5×5(1~10)·6×6(11~20) → 5×6/5×5/4×5(1~3) → 6×7~7×9(4~10) → 8×10(13~20). 가로≠세로 허용. 이후 세로 10 초과가 필요하므로 `maxBoardSize` 를 가로·세로로 분리 제안
4. **셀 크기 규칙 (프로그래머 W-015 에 바로 영향)**: 레퍼런스는 보드 크기와 무관하게 셀 간격이 일정 (화면 폭의 약 5.2%). 5칸 보드 = 화면 폭 22%, 8칸 = 37%. `areaWidthFraction 0.5` 로 늘리면 초반 보드가 레퍼런스보다 2배 이상 커짐 → 제안: `셀 = min(화면폭 × cellWidthFraction(0.052), 화면폭 × maxAreaFraction(0.9) ÷ 가로칸수)`
5. WORK 본문 (Lv1~2 직선만, Lv3 첫 꺾임) 과 스크린샷(Lv2 에 꺾임)이 달라 **스크린샷을 따름**
6. 레퍼런스 lv4 화살표 수는 7개가 아니라 **5개** (80칸을 5개가 모두 채움)

**팀장 에디터 할 일**
1. 레벨 파일 20개가 main 작업 트리에서 바뀌어 있음 → 커밋 필요 (기획자는 git 조작 안 함). 프로그래머의 `feat/path-arrows` 브랜치에 포함시키는 것을 권장
2. **W-015 머지 전에는 Game 씬에서 레벨 1~20 을 Play 하지 말 것** — 현재 main 의 Arrow 는 머리를 경로 끝이 아니라 dir 방향으로 가장 먼 칸으로 계산해 동작이 틀림
3. W-015 머지 후: Tools 의 Level Validator 창 → 전체 검증 → 오류 0 확인 후 `Record`

**다음**: W-012 (UI_FLOW v0.2). W-013 은 위 2·3번 결정과 W-015 검증기가 필요해 보류

### W-008 완료 (2026-09-17)
**산출물**: docs/LEVEL_DESIGN.md **v0.3** §7 — 레벨 11~20 JSON + 보드 그림 + 요약표
- 전부 프로젝트 `LevelValidator.Validate` 오류 0, `Record` 전후 solution·minTaps 동일
- 11~15: 6×6 Basic+Long, n 8→12, depth 4→7 (11 휴식, 15 고비)
- 16~20: Frozen 1→3개, n 9→13, depth 4→6. 16 은 Frozen 이 처음부터 쏠 수 있는 휴식 레벨, 17~20 은 막힌 Frozen 을 포함해 얼음은 깨져도 발사는 Block 되는 상황을 학습
- 생성기를 무작위 배치 → 경로 위 사슬 배치로 바꿈 (무작위로는 6×6 에서 depth 5 이상이 거의 안 나옴). 11~100 역방향 생성기 설계에 참고 가능
- Lv16 튜토리얼 손가락 대상: `a9`

**디렉터 확인 요청**
1. Lv20 depth 6 (§3 목표 7 미달) — 화살표 수·점유율로 보완. 플레이 테스트 후 판단 제안

**팀장 에디터 할 일**: 없음 (문서만 변경). 레벨 파일 저장은 W-006 에디터 툴 이후

**참고**: W-004·W-007 은 이미 다른 기획자 세션에서 완료·보고되어 있어 이번에는 중복 작업하지 않음

### W-007 완료 (2026-09-17)
**산출물**: docs/UI_FLOW.md v0.1
- 전체 흐름도, 공통 규칙(팝업 1개씩, Android 뒤로가기 표), Boot / Main(+레벨 선택) / Game(HUD 배치도) 화면 구성
- 팝업 6종 문구·버튼: 클리어, 실패(광고 이어하기 / 다시하기, 광고 미준비·이어하기 소진·중도 닫기 처리), 설정(Main/Game 차이), 메인으로 확인, 응모 코드(W-009 결과로 확정), 종료 확인
- 튜토리얼 표(Lv 1·2·6·16·26) — `TutorialConfig` ScriptableObject 로 관리 제안
- **광고 연결 지점 표 (§8)**: UI 이벤트 `LevelCleared` / `RetryPressed` / `ContinueRequested` → AdsManager 판단 → 콜백 후 이동. 프로그래머 W-005·#16 이 바로 참고 가능
- 문구 키 표 30여 개, 사운드 리스트 13종

**디렉터 확인 요청** (UI_FLOW §11)
1. 진동 토글 추가 (제안: 추가, 기본 켜짐, 하트 감소 시만)
2. 설정 팝업의 `다시하기`는 실패로 세지 않고 광고 없음
3. 레벨 중간 상태 저장 없음 (앱 종료 시 그 레벨 처음부터)
4. v1 언어 한국어만
5. 이미 깬 레벨을 다시 클리어해도 전면 광고 카운트에 포함

**팀장 에디터 할 일**: 지금은 없음. 캔버스·팝업 프리팹 구조 제안은 UI_FLOW §12

### W-004 완료 (2026-09-17)
**산출물**
- docs/LEVEL_FORMAT.md **v0.4** — Long 외 타입(Basic / Frozen / Locked / Key)은 정확히 1칸 (좌표계 설명 + 검증 규칙 2). 프로그래머 가정 (1)과 일치
- docs/LEVEL_DESIGN.md **v0.2**
  - 레벨 1~10 을 프로젝트 `LevelValidator.Validate` 로 재검증 → 전부 오류 0
  - 레벨 5·8·9 의 `solution` 을 검증기 결과 순서로 교체 (정답은 같고, 같은 층 안의 순서만 다름). 이제 `Record` 해도 JSON 이 바뀌지 않음
  - §3 보드 크기 11 / 21 / 40 → 승인 반영
  - 레벨 10 은 이전 보고대로 조건 완화 후 재생성한 결과(9·10 순서 교체) 그대로 유지
- docs/ISSUES.md #4 완료 조건 문구를 위 내용에 맞게 수정 (등록 전이면 반영, 이미 등록됐으면 이슈 본문만 맞추면 됨)

**디렉터 확인 요청**: 없음

**팀장 에디터 할 일**: 없음 (문서만 변경)

### 이전 보고 — 이슈 목록 · LEVEL_DESIGN 1~10 (2026-09-17, 채팅 보고를 옮김)
**산출물**
- docs/ISSUES.md — GitHub 이슈 초안 21건(M1 코어 7 / M2 화면·흐름 8 / M3 수익·출시 6) + 기획 트랙 5건. 라벨·우선순위·상태·완료 조건 포함. 권장 순서: #2·#3 병합 → #4 → #5 → #7 → 화면
- docs/LEVEL_DESIGN.md v0.1 — 난이도 지표 4개(n, free0, depth, fill), 톱니형 곡선 원칙, 1~100 구간 계획, 레벨 1~10 JSON + 보드 그림. 1~10 은 제 시뮬레이터와 프로젝트 `LevelLoader`·`FireResolver` 양쪽으로 풀이 확인. 난이도 3개·2층(Lv1) → 9개·7층(Lv10), Lv6 은 Long 도입 휴식 레벨
- docs/CLAUDE_DESKTOP.md 다음 할 일 갱신

**그 전 작업 요약**
- LEVEL_FORMAT v0.1 예시 레벨이 순환 막힘으로 풀 수 없음을 발견 → v0.2 에서 교체, 해결 가능 판정을 탐욕 시뮬레이션으로 변경
- GAME_RULES v0.4 검토 7건 → v0.5 / v0.5.1 반영 확인, §9 HUD·§5 별점 잔여 불일치 지적 → v0.5.1 해결

**디렉터 확인 요청 (당시)** — 모두 처리됨
- 보드 크기 전환 11 / 21 / 40 → v0.5.2 승인
- CLAUDE.md 의 Undo·힌트 비용 잔재 → 제거됨
- 광고 SDK → AdMob + Unity Ads 확정
- 응모 코드 기존 코드 위치 → W-009 조사로 진행

**팀장 에디터 할 일**: 없음

