namespace NanaArrow.Core
{
    /// <summary>ANALYTICS level_start.start_reason.</summary>
    public enum LevelStartReason
    {
        First,
        Next,
        Select,
        RetryHud,
        RetryFail,
    }
}
