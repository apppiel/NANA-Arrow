namespace NanaArrow.UI.Main
{
    /// <summary>레벨 선택 칸 상태 (UI_FLOW §4): 클리어(체크) / 다음 도전(강조) / 잠김(자물쇠).</summary>
    public enum LevelCellState
    {
        Cleared,
        Next,
        Locked
    }
}
